<Page x:Class="PracticeAccountingApp.Pages.PracticesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource PrimaryBrush}">

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Ведомости практик" Style="{StaticResource PageTitleText}"/>

        <!-- Action Bar -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,24" Padding="18">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="360" Style="{StaticResource InputBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Content="Добавить ведомость" Margin="20,0,0,0"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding OpenAddCommand}"/>
            </StackPanel>
        </Border>

        <!-- Card List -->
        <ListView Grid.Row="2" ItemsSource="{Binding Practices}"
                  Background="Transparent" BorderThickness="0">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="10"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Width="390" Height="185" Style="{StaticResource CardStyle}" Padding="18">
                        <!-- Double Click Edit -->
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick"
                                          Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                          CommandParameter="{Binding}"/>
                        </Border.InputBindings>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="{Binding PracticeType}" FontSize="16.5" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"/>
                            <TextBlock Grid.Row="1" Text="{Binding ModuleName}" 
                                       Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,2"/>
                            <TextBlock Grid.Row="2" Text="{Binding Group}" FontWeight="Medium"
                                       Foreground="{StaticResource AccentBrush}"/>

                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" 
                                        VerticalAlignment="Bottom">
                                <Button Content="Изменить" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                                        Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="Удалить" Style="{StaticResource GhostButton}"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>

                            <TextBlock Grid.Row="3" Text="{Binding Start, StringFormat='с {0:dd.MM}'}"
                                       Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Left"
                                       VerticalAlignment="Bottom"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>