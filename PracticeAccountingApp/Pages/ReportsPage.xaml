<Page x:Class="PracticeAccountingApp.Pages.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource PrimaryBrush}">

    <Grid Margin="28">
        <TextBlock Text="Отчеты студентов" Style="{StaticResource PageTitleText}"/>

        <!-- Здесь будет полноценная таблица отчетов позже -->
        <Border Style="{StaticResource CardStyle}" Margin="0,30,0,0" Padding="30">
            <TextBlock Text="Раздел в разработке..." 
                       FontSize="18" HorizontalAlignment="Center"
                       Foreground="{StaticResource MutedTextBrush}"/>
        </Border>
    </Grid>
</Page>