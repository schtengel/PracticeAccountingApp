<Page x:Class="PracticeAccountingApp.Pages.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource PrimaryBrush}">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid Margin="28,24,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Отчёты студентов" 
                   Style="{StaticResource PageTitleText}" Margin="0 0 0 12"/>

        <!-- Action Bar -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Width="360" HorizontalAlignment="Left"
                         Margin="0 0 12 0"
                         Style="{StaticResource InputBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Поиск по студенту, группе, типу практики"/>

                <Button Grid.Column="1"
                        Content="＋  Добавить отчёт"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding OpenAddCommand}"
                        Visibility="{Binding CanManagePractices,
                                    Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Reports}"
                      Style="{StaticResource ModernDataGrid}"
                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                      ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      CellStyle="{StaticResource DataGridCellStyle}"
                      x:Name="ReportsGrid">

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource DataGridRowStyle}">
                        <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Студент"
                                        Binding="{Binding StudentName}"
                                        Width="Auto"/>

                    <DataGridTextColumn Header="Группа"
                                        Binding="{Binding Group}"
                                        Width="Auto"/>

                    <DataGridTextColumn Header="Тип практики"
                                        Binding="{Binding PracticeType}"
                                        Width="Auto"/>

                    <!-- Оценка с цветом -->
                    <DataGridTemplateColumn Header="Оценка" Width="Auto" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource BadgeStyle}"
                                        HorizontalAlignment="Center" Margin="0,4">
                                    <TextBlock Text="{Binding GradeDisplay}"
                                               Style="{StaticResource BadgeText}"
                                               Foreground="{Binding GradeColor}"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Дата сдачи"
                                        Binding="{Binding SubmissionDate, StringFormat=dd.MM.yyyy}"
                                        Width="Auto"/>

                    <!-- Файл — показываем только имя, не полный путь -->
                    <DataGridTemplateColumn Header="Файл" Width="Auto" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FilePath}"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding FilePath}"
                                           Foreground="{StaticResource MutedTextBrush}"
                                           FontSize="13"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Действия -->
                    <DataGridTemplateColumn Header="Действия" Width="120" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                            Visibility="{Binding DataContext.CanManagePractices,
                                                         RelativeSource={RelativeSource AncestorType=DataGrid},
                                                         Converter={StaticResource BoolToVisibilityConverter}}">

                                    <Button Content="✏"
                                            Style="{StaticResource GhostButton}"
                                            Padding="8,5" FontSize="14"
                                            ToolTip="Редактировать"
                                            Command="{Binding DataContext.EditCommand,
                                                      RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>

                                    <Button Content="🗑"
                                            Style="{StaticResource DangerButton}"
                                            Padding="8,5" FontSize="14"
                                            ToolTip="Удалить"
                                            Command="{Binding DataContext.DeleteCommand,
                                                      RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
