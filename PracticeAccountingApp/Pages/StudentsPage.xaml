<Page x:Class="PracticeAccountingApp.Pages.StudentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource PrimaryBrush}">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid Margin="28,24,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Студенты" Style="{StaticResource PageTitleText}" 
                   Margin="0 0 0 12"/>

        <!-- Action Bar -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Width="320" HorizontalAlignment="Left"
                         Style="{StaticResource InputBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Поиск по ФИО или группе"/>

                <Button Grid.Column="1"
                        Content="＋  Добавить студента"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding OpenAddCommand}"
                        Visibility="{Binding CanManageGroupsAndStudents,
                                    Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <!-- Cards -->
        <ListView ItemsSource="{Binding Students}"
                  Grid.Row="2"
                      Background="Transparent" BorderThickness="0"
                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0,0,14,14"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Width="370" Style="{StaticResource CardStyle}"
                                Padding="18" Margin="0"
                                Cursor="Hand">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick"
                                              Command="{Binding DataContext.EditCommand,
                                                        RelativeSource={RelativeSource AncestorType=ListView}}"
                                              CommandParameter="{Binding}"/>
                        </Border.InputBindings>

                        <StackPanel>
                            <!-- Заголовок карточки: группа + ID -->
                            <Grid Margin="0,0,0,10">
                                <Border Style="{StaticResource BadgeStyle}" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Group}" Style="{StaticResource BadgeText}"/>
                                </Border>
                                <TextBlock Text="{Binding Id, StringFormat='#{0}'}"
                                               HorizontalAlignment="Right"
                                               FontSize="12"
                                               Foreground="{StaticResource MutedTextBrush}"
                                               VerticalAlignment="Center"/>
                            </Grid>

                            <!-- ФИО -->
                            <TextBlock Text="{Binding FullName}"
                                           FontSize="16" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextBrush}"
                                           TextWrapping="Wrap"/>

                            <!-- Дата рождения -->
                            <TextBlock Text="{Binding BirthDate, StringFormat='Дата рождения: {0:dd.MM.yyyy}'}"
                                           FontSize="13"
                                           Foreground="{StaticResource MutedTextBrush}"
                                           Margin="0,8,0,0"/>

                            <!-- Разделитель -->
                            <Border Height="1" Margin="0,12,0,10"
                                        Background="{StaticResource DividerBrush}"/>

                            <!-- Действия -->
                            <Grid>
                                <Button Content="✏ Изменить"
                                            HorizontalAlignment="Left"
                                            Style="{StaticResource GhostButton}"
                                            Padding="8,5"
                                            FontSize="13"
                                            Visibility="{Binding DataContext.CanManageGroupsAndStudents,
                                                         RelativeSource={RelativeSource AncestorType=ListView},
                                                         Converter={StaticResource BoolToVisibilityConverter}}"
                                            Command="{Binding DataContext.EditCommand,
                                                      RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"/>

                                <Button Content="🗑 Удалить"
                                            HorizontalAlignment="Right"
                                            Style="{StaticResource DangerButton}"
                                            Padding="8,5"
                                            FontSize="13"
                                            Visibility="{Binding DataContext.CanManageGroupsAndStudents,
                                                         RelativeSource={RelativeSource AncestorType=ListView},
                                                         Converter={StaticResource BoolToVisibilityConverter}}"
                                            Command="{Binding DataContext.DeleteCommand,
                                                      RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
