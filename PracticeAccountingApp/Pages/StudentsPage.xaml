<Page x:Class="PracticeAccountingApp.Pages.StudentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource PrimaryBrush}">

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Студенты" Style="{StaticResource PageTitleText}"/>

        <!-- Action Bar -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,20" Padding="16">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="320" Style="{StaticResource InputBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Content="Добавить студента" Margin="20,0,0,0"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding OpenAddCommand}"/>
            </StackPanel>
        </Border>

        <!-- Students List -->
        <ListView Grid.Row="2" ItemsSource="{Binding Students}"
                  Background="Transparent" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="10"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Width="350" Height="185" Style="{StaticResource CardStyle}" Padding="18">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick"
                                          Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                          CommandParameter="{Binding}"/>
                        </Border.InputBindings>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="{Binding Id}" Foreground="{StaticResource AccentBrush}" FontSize="13"/>
                            <TextBlock Grid.Row="1" Text="{Binding FullName}" FontSize="17" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}" Margin="0,8,0,4"/>
                            <TextBlock Grid.Row="2" Text="{Binding Group}" Foreground="{StaticResource MutedTextBrush}"/>
                            <TextBlock Grid.Row="3" Text="{Binding BirthDate, StringFormat='Дата рождения: {0:dd.MM.yyyy}'}"
                                       Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0"/>

                            <Button Content="Удалить" Style="{StaticResource GhostButton}"
                                    HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                    Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>