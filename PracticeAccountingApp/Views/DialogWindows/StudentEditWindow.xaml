<Window x:Class="PracticeAccountingApp.Views.DialogWindows.StudentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование студента" 
        Height="520" Width="560"
        MinHeight="480" MinWidth="520"
        WindowStartupLocation="CenterScreen" 
        Background="{StaticResource PrimaryBrush}">

    <Border Margin="24" Background="{StaticResource SecondaryBrush}" 
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            CornerRadius="16" Padding="32">

        <StackPanel>
            <TextBlock Text="Редактирование студента" 
                       Style="{StaticResource SectionTitleText}"
                       HorizontalAlignment="Center" Margin="0,0,0,24"/>

            <TextBlock Text="ФИО" Margin="4,0,0,6" Foreground="{StaticResource MutedTextBrush}"/>
            <TextBox Style="{StaticResource InputBox}" Text="{Binding FullName}"/>

            <TextBlock Text="Группа" Margin="4,16,0,6" Foreground="{StaticResource MutedTextBrush}"/>
            <ComboBox Style="{StaticResource ComboBoxInput}" 
                      ItemsSource="{Binding Groups}"
                      SelectedItem="{Binding Group}"/>

            <TextBlock Text="Дата рождения (дд.мм.гггг)" Margin="4,16,0,6" 
                       Foreground="{StaticResource MutedTextBrush}"/>
            <TextBox Style="{StaticResource InputBox}" 
                     Text="{Binding BirthDateString, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Сохранить" 
                    Margin="0,32,0,0" Height="48"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Border>
</Window>