<Window x:Class="PracticeAccountingApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Учет ведомостей по практикам"
        Height="800" Width="1250"
        MinHeight="650" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Background="{StaticResource PrimaryBrush}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="268"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ═══ SIDEBAR ═══ -->
        <Border Grid.Column="0"
                Background="{StaticResource SecondaryBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,1,0">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo -->
                <StackPanel Margin="24,28,24,20">
                    <TextBlock Text="МЦПК"
                               FontSize="38" FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Text="Учёт практик"
                               FontSize="13" Margin="2,4,0,0"
                               Foreground="{StaticResource MutedTextBrush}"/>
                </StackPanel>

                <!-- Divider -->
                <Border Grid.Row="0" Height="1" VerticalAlignment="Bottom"
                        Margin="16,0" Background="{StaticResource DividerBrush}"/>

                <!-- Navigation -->
                <ListBox Grid.Row="1"
                         x:Name="NavigationMenu"
                         Margin="10,12,10,0"
                         Background="Transparent"
                         BorderThickness="0"
                         ItemContainerStyle="{StaticResource NavigationItemStyle}"
                         SelectionChanged="NavigationMenu_SelectionChanged">

                    <ListBoxItem Tag="Pages/HomePage.xaml">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="🏠" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Главная" FontSize="15" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Tag="Pages/StudentsPage.xaml">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="🎓" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Студенты" FontSize="15" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Tag="Pages/GroupsPage.xaml">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="👥" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Группы" FontSize="15" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Tag="Pages/PracticesPage.xaml">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="📚" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Ведомости" FontSize="15" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Tag="Pages/ReportsPage.xaml">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="📄" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Отчёты" FontSize="15" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>

                <!-- User Panel -->
                <Border Grid.Row="2" Margin="14,0,14,16" Padding="14,12"
                        Background="{StaticResource PrimaryBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center">
                            <!-- Гость или логин -->
                            <TextBlock FontWeight="SemiBold" FontSize="14"
                                       Foreground="{StaticResource TextBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding CurrentUser.Login}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsGuest}" Value="True">
                                                <Setter Property="Text" Value="Гость"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- Роль -->
                            <TextBlock FontSize="12" Margin="0,3,0,0"
                                       Foreground="{StaticResource MutedTextBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding CurrentUser.Role.RoleName}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsGuest}" Value="True">
                                                <Setter Property="Text" Value="Без авторизации"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>

                        <!-- Иконка пользователя -->
                        <Border Grid.Column="1" Width="34" Height="34"
                                Background="{StaticResource AccentLightBrush}"
                                CornerRadius="17"
                                VerticalAlignment="Center">
                            <TextBlock Text="👤" FontSize="14"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- ═══ MAIN AREA ═══ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="68"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <Border Grid.Row="0"
                    Background="{StaticResource SecondaryBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <Grid Margin="28,0">
                    <TextBlock Text="Учёт ведомостей по практикам"
                               VerticalAlignment="Center"
                               FontSize="20" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"/>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">

                        <Button Content="＋ Создать пользователя"
                                Style="{StaticResource SecondaryButton}"
                                Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVisibilityConverter}}"
                                Click="OpenCreateUser"
                                Margin="0,0,10,0"/>

                        <Button Content="Выйти"
                                Style="{StaticResource GhostButton}"
                                Click="LogOutButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Page Frame -->
            <Border Grid.Row="1" Margin="20"
                    Background="{StaticResource PrimaryBrush}"
                    CornerRadius="14"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1">
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
            </Border>
        </Grid>
    </Grid>
</Window>
