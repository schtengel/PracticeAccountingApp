<Window x:Class="PracticeAccountingApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Учет ведомостей по практикам"
        Height="800" Width="1250"
        MinHeight="650" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="{StaticResource PrimaryBrush}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Grid.Column="0" Background="{StaticResource SecondaryBrush}" 
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- LOGO -->
                <StackPanel Margin="28,32,28,20">
                    <TextBlock Text="МЦПК" FontSize="42" FontWeight="Bold" 
                               Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Text="Учет практик" FontSize="15" Margin="2,4,0,0"
                               Foreground="{StaticResource MutedTextBrush}"/>
                </StackPanel>

                <!-- NAVIGATION -->
                <ListBox Grid.Row="1" x:Name="NavigationMenu" Margin="12,0"
                         Background="Transparent" BorderThickness="0"
                         ItemContainerStyle="{StaticResource NavigationItemStyle}"
                         SelectionChanged="NavigationMenu_SelectionChanged">

                    <ListBoxItem Tag="Pages/HomePage.xaml">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏠" FontSize="18"/>
                            <TextBlock Text="Главная" FontSize="17" Margin="14,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem Tag="Pages/StudentsPage.xaml">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🎓" FontSize="18"/>
                            <TextBlock Text="Студенты" FontSize="17" Margin="14,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem Tag="Pages/GroupsPage.xaml">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👥" FontSize="18"/>
                            <TextBlock Text="Группы" FontSize="17" Margin="14,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem Tag="Pages/PracticesPage.xaml">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📚" FontSize="18"/>
                            <TextBlock Text="Ведомости" FontSize="17" Margin="14,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem Tag="Pages/ReportsPage.xaml">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📄" FontSize="18"/>
                            <TextBlock Text="Отчеты" FontSize="17" Margin="14,0,0,0"/>
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>

                <!-- USER PANEL -->
                <Border Grid.Row="2" Margin="20" Padding="16"
                        Background="{StaticResource PrimaryBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="Текущий пользователь" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"/>
                        <TextBlock Text="{Binding CurrentUser.Login}" Margin="0,6,0,0"
                                   Foreground="{StaticResource MutedTextBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TOP BAR -->
            <Border Grid.Row="0" Background="{StaticResource SecondaryBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                <Grid Margin="32,0">
                    <TextBlock Text="Учет ведомостей по практикам"
                               VerticalAlignment="Center"
                               FontSize="26" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                        <Button Content="Создать пользователя" Width="170"
                                Style="{StaticResource SecondaryButton}"
                                Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVisibilityConverter}}"
                                Click="OpenCreateUser" Margin="0,0,12,0"/>

                        <Button Content="Выйти" Width="110"
                                Style="{StaticResource SecondaryButton}"
                                Click="LogOutButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- PAGE CONTENT -->
            <Border Grid.Row="1" Margin="24" Background="{StaticResource PrimaryBrush}"
                    CornerRadius="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
            </Border>
        </Grid>
    </Grid>
</Window>